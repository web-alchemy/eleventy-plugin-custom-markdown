<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
</head>
<body>
  <h1 class="title">
    {{ title | safe }}
  </h1>

  <pre>{{ tableOfContents | dump(2) }}</pre>

  {% if excerpt %}
    <div class="excerpt">
      {{ excerpt | safe }}
    </div>
  {% endif %}

  <h2 class="heading">
    {{ heading }}
  </h2>

  <pre class="categories">
    {{ categories }}
  </pre>

  <div class="content">
    {{ content | safe }}
  </div>
</body>
</html>